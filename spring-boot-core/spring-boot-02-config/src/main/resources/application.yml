server:
  port: 8081
  tomcat:
    uri-encoding: UTF-8
  servlet:
    context-path: /config
spring:
  profiles:
    active: prod

---

server:
  port: 8083
spring:
  profiles: dev

---

server:
  port: 8084
spring:
  profiles: prod